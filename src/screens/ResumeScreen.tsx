import React from 'react';
import {SafeAreaView} from 'react-native';
import ResumeContainer from '../containers/ResumenContainer';

export const ResumeScreen = () => {
  return (
    <SafeAreaView>
      <ResumeContainer />
    </SafeAreaView>
  );
};

export default ResumeScreen;
